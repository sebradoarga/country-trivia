import React from "react";
import ReturnButtonStyles from "./ReturnButton.module.css";

const ReturnButton = () => {
  return <button className={ReturnButtonStyles.btn}>Go back</button>;
};

export default ReturnButton;
